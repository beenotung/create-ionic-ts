{
  "name": "template",
  "version": "0.0.0",
  "type": "commonjs",
  "description": "",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "run-p watch serve",
    "serve": "serve-lite --app www",
    "android": "npm run build && npx cap copy android",
    "ios": "npm run build && npx cap copy ios",
    "test": "tsc --noEmit",
    "build": "esbuild src/app.ts --bundle --outfile=www/bundle.js",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "esbuild": "^0.27.3",
    "npm-run-all": "^4.1.5",
    "rimraf": "^6.1.3",
    "serve-lite": "^1.6.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@capacitor/android": "^8.2.0",
    "@capacitor/cli": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/preferences": "^8.0.1",
    "@ionic/core": "^8.7.18"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
